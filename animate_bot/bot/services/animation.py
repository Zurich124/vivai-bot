"""
Модуль генерации анимации.

Это единственное место, которое нужно реализовать под ваш источник
анимации (собственная модель, самописный алгоритм, локальный сервис и т.д.).

Контракт простой:
    вход  — путь к фото (или байты) на диске,
    выход — путь к готовому видео/gif (или байты), который бот отправит пользователю.

Всё остальное (Telegram-логика, меню, проверки) уже реализовано в handlers/
и трогать их не нужно — просто допишите функцию animate_photo ниже.
"""

from pathlib import Path


class AnimationError(Exception):
    """Выбрасывайте это исключение, если генерация не удалась —
    бот покажет пользователю понятное сообщение об ошибке."""


async def animate_photo(input_path: Path, user_id: int) -> Path:
    """
    Принимает путь к загруженному фото, возвращает путь к результату.

    Реализуйте здесь вызов вашего источника анимации, например:

        result_path = await my_animation_engine.process(input_path)
        return result_path

    Пока функция не реализована — бот будет сообщать пользователю,
    что генерация недоступна.
    """
    raise AnimationError(
        "Источник анимации ещё не подключён. "
        "Реализуйте функцию animate_photo() в bot/services/animation.py"
    )
